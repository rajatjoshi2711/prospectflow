import { ComingSoon } from "@/components/coming-soon";

export default function ChannelPartnersDashboardPage() {
  return (
    <ComingSoon
      title="All channel partners"
      description="Channel partner match dashboards arrive once channel partner matching ships."
      phase="Phase 4 — ICP / channel partner matching"
    />
  );
}
