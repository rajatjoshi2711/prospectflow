import { ComingSoon } from "@/components/coming-soon";

export default function ProspectAskPage() {
  return (
    <ComingSoon
      title="ProspectAsk"
      description="Ask free-form questions across your imported connections, ICP matches, and campaign status, grounded in real tool-call results."
      phase="Phase 6 — AI depth"
    />
  );
}
