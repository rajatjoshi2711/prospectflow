import { ComingSoon } from "@/components/coming-soon";

export default function IcpsDashboardPage() {
  return (
    <ComingSoon
      title="All ICPs"
      description="Paginated, company-grouped views of your Ideal Customer Profile matches arrive once ICP matching ships."
      phase="Phase 4 — ICP / channel partner matching"
    />
  );
}
