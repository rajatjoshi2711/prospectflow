import { ComingSoon } from "@/components/coming-soon";

export default function OrgDashboardPage() {
  return (
    <ComingSoon
      title="Org dashboard"
      description="A connections-over-time graph and org-wide quick suggestions arrive later in the build."
      phase="Phase 6 — AI depth"
    />
  );
}
