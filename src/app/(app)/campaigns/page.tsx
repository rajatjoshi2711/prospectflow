import { ComingSoon } from "@/components/coming-soon";

export default function CampaignsPage() {
  return (
    <ComingSoon
      title="Campaigns"
      description="Upload an Excel lead list and track outreach status from request sent through ongoing conversation."
      phase="Phase 5 — campaigns"
    />
  );
}
