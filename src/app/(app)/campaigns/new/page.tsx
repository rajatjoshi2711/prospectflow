import { ComingSoon } from "@/components/coming-soon";

export default function NewCampaignPage() {
  return (
    <ComingSoon
      title="New campaign"
      description="Excel upload with automatic LinkedIn-URL column detection arrives with campaigns."
      phase="Phase 5 — campaigns"
    />
  );
}
