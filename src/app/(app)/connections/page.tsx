import { ComingSoon } from "@/components/coming-soon";

export default function ConnectionsDashboardPage() {
  return (
    <ComingSoon
      title="All connections"
      description="A searchable, org-wide view of every imported LinkedIn connection arrives alongside the core dashboards."
      phase="Phase 3 — diffing & core dashboards"
    />
  );
}
