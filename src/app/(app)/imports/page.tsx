import { ComingSoon } from "@/components/coming-soon";

export default function ImportsPage() {
  return (
    <ComingSoon
      title="Imports"
      description="Upload your LinkedIn export zip and browse version history across every past import."
      phase="Phase 2 — ingestion"
    />
  );
}
